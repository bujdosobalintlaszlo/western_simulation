using System;
using System.Collections;
using System.Collections.Generic;
using System.Reflection;
using UnityEngine;
using UnityEngine.UI;

public class Sheriff
{
    /*
    int stepsCount;
    public Sheriff(int health, int damagePower, double movementSpeed, double hitSpeed, int goldCount, int xIndex, int yIndex, int zIndex) : base(health, damagePower, movementSpeed, hitSpeed, goldCount, xIndex, yIndex, zIndex)
    {
        stepsCount = 0;
    }
    */
}
